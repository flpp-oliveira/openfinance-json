<svg width="400" height="100" viewBox="0 0 400 100" xmlns="http://www.w3.org/2000/svg" fill="none">
  <!-- Circular icon with gradient -->
  <defs>
    <linearGradient id="grad1" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#28C2D1"></stop>
      <stop offset="100%" stop-color="#0077B6"></stop>
    </linearGradient>
  </defs>

  <!-- Outer Circle -->
  <circle cx="50" cy="50" r="36" fill="url(#grad1)"></circle>

  <!-- Inner JSON icon -->
  <text x="36" y="58" font-family="monospace" font-size="26" fill="#fff" font-weight="bold">{ }</text>

  <!-- Brand Text -->
  <text x="100" y="55" font-family="Segoe UI, sans-serif" font-size="30" font-weight="700" fill="#1F2937">
    JSONify
  </text>
  <text x="100" y="80" font-family="Segoe UI, sans-serif" font-size="16" font-weight="500" fill="#6B7280">
    Open Finance
  </text>
</svg>
